import Card from "@/components/ui/Card";
import { text } from "@/lib/utilities";

export default function TerritoriesCard() {
  return (
    <Card title={text('feature_buildings.card_territories.title')} style="elevated" width="w-33">
        <p></p>
    </Card>
  )
}